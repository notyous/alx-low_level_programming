#include "main.h"
/**
 * _isdigit - checks digit
 *
 * @c: character checked
 * Return: 1 if digit, 0 if not
 */

int _isdigit(int c)
{
	if (c >= 48 && c < 58)
		return (1);
	else
		return (0);
	return (0);
}
